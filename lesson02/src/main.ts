let myName: string = 'Abhijit'
let meaningOfLife: number
let isLoading: boolean
let album: any

myName = 'John'
meaningOfLife = 42
isLoading = false
album = 1984
album = 'Van Halen'

const sum = (a: number, b: number) => {
  return a + b
}

let postId: string | number
let isActive: number | boolean

let re: RegExp = /\w+/g
